machine:
  timezone:
    Asia/Tokyo # Set the timezone
  ruby:
    version:
      2.1.1

database:
  override:
    - ./bin/dynamodb:
      background: true
    - bundle exec rake db:migrate

test:
  override:
    - bundle exec rake spec
